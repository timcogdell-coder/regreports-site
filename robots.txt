# Pre-launch: discourage crawling while the site is in testing.
# The authoritative signal is the <meta name="robots" content="noindex"> on each
# page. To go live, remove the meta tags and replace this with "Disallow:" (allow all).
User-agent: *
Disallow: /
